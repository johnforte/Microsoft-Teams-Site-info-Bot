{
  "name": "teams-site-checker-bot",
  "version": "1.0.0",
  "description": "Checks the url and gets stats, as well as post a picture of the page",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "botbuilder": "^3.4.4",
    "request": "^2.79.0",
    "restify": "^4.3.0"
  }
}
